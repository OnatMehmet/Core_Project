@model EntityLayer.Concrete.Service
@{
    ViewData["Title"] = "EditService";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

@*
<div class="container">
    <h1>Hizmet Düzenleme</h1>

    @using (Html.BeginForm("EditService", "Service", FormMethod.Post))
    {
        @Html.HiddenFor(x=>x.ServiceId)
        <br />

        @Html.Label("Hizmet Başlık")
        @Html.TextBoxFor(x=>x.Title, new{@class ="form-control"})
        <br />
        @Html.Label("Resim URL")
        @Html.TextBoxFor(x=>x.ImageUrl, new{@class ="form-control"})
        <br />
        <button type="submit" class="btn btn-primary btn-block btn-lg">Hizmet Güncelle</button>

    }

</div>*@

<div class="col-12 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <h4 class="card-title">Hizmet Düzenleme</h4>

            <form class="forms-sample" method="post">
                @Html.HiddenFor(x=>x.ServiceId)
                <div class="form-group">
                    @Html.Label("Hizmet Başlık")
                    @Html.TextBoxFor(x=>x.Title, new{@class ="form-control"})
                </div>

                <div class="form-group">
                    @Html.Label("Resim URL")
                    @Html.TextBoxFor(x=>x.ImageUrl, new{@class ="form-control"})
                </div>

                <button type="submit" class="btn btn-primary mr-2">Hizmet Güncelle</button>
                <button href="#" class="btn btn-dark">İptal</button>
            </form>

        </div>
    </div>
</div>

